{
  "name": "linuxad-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Tableau de bord de suivi de migration Windows -> Linux (Lot-et-Garonne Numerique)",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "install:all": "npm install && npm --prefix client install"
  }
}
